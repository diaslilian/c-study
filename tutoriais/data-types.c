#include <stdio.h>
#include <stdlib.h>

int main()
{
  // int = para numeros inteiros
  int age = 40;

  // double = numeros decimais
  double gpa = 3.6;

  // char com aspas simples '' = só recebe um caracter
  char grade = 'A';

  // char array[] e com aspas duplas "" = recebe uma string, uma coleção de caracteres
  char phrase[] = "String of characters";

  // pausar aplicação
  system("pause");

  return 0;
}